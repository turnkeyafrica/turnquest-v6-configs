server:
  port: 1040

app:
  auth:
    server:
      realm-url: https://evolve.keycloak.caavagroup.com/realms/turnquest-version-six

spring.cloud.kubernetes:
  discovery:
    enabled: true
    all-namespaces: false

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
