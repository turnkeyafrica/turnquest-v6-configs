server:
  port: 1027

camunda.bpm:
  admin-user:
    id: admin
    password: admin

camunda.bpm.database:
  schema-update: true
  type: h2
  jdbc-batch-processing: true

spring.cloud.kubernetes:
  discovery:
    enabled: true
    all-namespaces: false

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
